name: pheno-maps
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.10
  - numpy
  - pandas
  - scipy
  - pip
  - pip:
      - anndata
      - leidenalg
      - manhole
      - scanpy
      - zarr
      - muon
      - mudata
      - tables
      - pyyaml
      - requests