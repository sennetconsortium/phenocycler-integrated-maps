name: pheno-maps
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.10
  - numpy
  - pandas
  - scipy
  - pip
  - dask
  - pip:
      - anndata
      - leidenalg
      - manhole
      - scanpy
      - zarr
      - muon
      - mudata
      - tables
      - pyyaml
      - requests
      - cupy-cuda12x
      - tensorflow
      - cuml-cu12
      - nvidia-cuda-nvcc
      - cmake
      - numba